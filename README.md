# lihkg channel classifier

a lihkg channel classifer model that fine-tuning a BERT Mandarin Base model and BERT Cantonese Base model.

1. use Predict.ipynb, input the title then predict the channel.
